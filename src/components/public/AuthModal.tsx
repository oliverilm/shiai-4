import React, {useState} from 'react'

const AuthModal = () => {
    const [open, setOpen] = useState<boolean>(false)
    return (
        <div>
            
        </div>
    )
}

export default AuthModal;
